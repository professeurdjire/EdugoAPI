# ===============================
# SERVER CONFIGURATION
# ===============================
# server.port=8091  # Commented out to use dynamic port assignment
server.servlet.context-path=/api

# ===============================
# SPRINGDOC / SWAGGER CONFIGURATION
# ===============================
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui/index.html
springdoc.default-produces-media-type=application/json
springdoc.default-consumes-media-type=application/json
springdoc.paths-to-match=/api/**
springdoc.packages-to-scan=com.example.edugo.controller

# Jackson to avoid failures on Hibernate proxies during OpenAPI serialization
spring.jackson.serialization.fail-on-empty-beans=false

# ===============================
# DATASOURCE CONFIGURATION (MySQL)
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/edugodatabase?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / HIBERNATE CONFIGURATION
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.batch_versioned_data=true

# ===============================
# LOGGING CONFIGURATION
# ===============================
logging.level.com.example.livrequiz=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.web=INFO
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.web.access=DEBUG

# ===============================
# SPRING MVC CONFIGURATION
# ===============================
spring.mvc.format.date=yyyy-MM-dd
spring.mvc.format.date-time=yyyy-MM-dd HH:mm:ss
spring.mvc.format.time=HH:mm:ss

# ===============================
# FILE UPLOAD CONFIGURATION
# ===============================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# ===============================
# INTERNATIONALIZATION
# ===============================
spring.web.locale=fr
spring.web.locale-resolver=fixed

# ===============================
# CORS CONFIGURATION (pour le frontend)
# ===============================
# spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:4200
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.web.cors.allowed-headers=*
# spring.web.cors.allow-credentials=true

# ===============================
# SECURITY (Basic -  configurer avec Spring Security)
# ===============================
# spring.security.user.name=admin
# spring.security.user.password=admin

# ===============================
# ACTUATOR (pour le monitoring)
# ===============================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# ===============================
# CACHE CONFIGURATION
# ===============================
spring.cache.type=simple
spring.cache.cache-names=livres,users,quizzes

# ===============================
# THYMELEAF (si utilisation de templates)
# ===============================
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.servlet.content-type=text/html
spring.thymeleaf.cache=false

# ===============================
# EMAIL CONFIGURATION
# ===============================
# Configuration pour Gmail
# IMPORTANT : Pour Gmail, vous devez utiliser un "App Password" (pas votre mot de passe normal)
# Créez un App Password : https://myaccount.google.com/apppasswords
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=professeurhamidoudjire@gmail.com
# Remplacez par votre App Password Gmail (sans espaces)
# IMPORTANT : Supprimez les espaces de votre App Password Gmail
spring.mail.password=tvjpbcrzreffbeak
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Email par défaut pour l'expéditeur (utilisé dans EmailService)
# Si vous voulez utiliser un autre email comme expéditeur, modifiez cette ligne
# spring.mail.from=no-reply@edugo.ml

# ===============================
# FILE STORAGE CONFIGURATION
# ===============================
app.file.upload-dir=./uploads
app.file.max-size=10485760

# ===============================
# CUSTOM APPLICATION PROPERTIES
# ===============================
app.name=LivreQuiz-API
app.version=1.0.0
app.description=API pour la gestion de livres et quiz educatifs

# JWT Configuration
app.jwt.secret=mySecretKeyForJWTTokenGenerationThatShouldBeLongEnough
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000

# Password Reset Configuration
app.password-reset.token-expiration-hours=24
app.password-reset.base-url=http://localhost:8080

# Development mode - can be enabled by setting DEV_MODE=true as environment variable
# This will be detected by SecurityConfig to bypass authentication in development

# ===============================
# GEMINI CONFIGURATION
# ===============================
gemini.apiKey=${GEMINI_API_KEY:}

openrouter.apiKey=${OPENROUTER_API_KEY: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}
openrouter.model=openrouter/auto

server.address=0.0.0.0
server.port=8080

# ===============================
# ONESIGNAL CONFIGURATION
# ===============================
# Remplacez ces valeurs par vos clés OneSignal réelles
# Obtenez-les depuis : https://dashboard.onesignal.com/apps
# 
# Option 1 : Mettre directement vos clés ici (remplacez les valeurs ci-dessous)
onesignal.app.id=07b64c22-48ee-4981-9bf5-df3d231a5e45
onesignal.rest.api.key=exeq4dmc5u2b4x23u5tamxmup
onesignal.enabled=true

# Option 2 : Utiliser des variables d'environnement (plus sécurisé pour la production)
# Décommentez les lignes ci-dessous et commentez les lignes ci-dessus
# onesignal.app.id=${ONESIGNAL_APP_ID:}
# onesignal.rest.api.key=${ONESIGNAL_REST_API_KEY:}
# onesignal.enabled=${ONESIGNAL_ENABLED:true}