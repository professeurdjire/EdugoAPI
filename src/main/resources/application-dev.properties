# Development profile configuration
# This profile bypasses authentication for easier development and testing

# Server configuration
server.port=8089
server.servlet.context-path=/api

# Enable development mode for security
# This will be detected by SecurityConfig to bypass authentication